[user]
my_name_is = "${NAME|Super Unknown} <${EMAIL|in@cogni.to}>"
my_username_is = $USER

[project]
type = python
scm  = ${SCM|git}

[other]
agent_pid = $( ( ${SSH_AGENT_PID|0} + 1 )
 * 3 / 2
 >
  ${MIN_VALUE|2000} )

eof = "No trailing newline"